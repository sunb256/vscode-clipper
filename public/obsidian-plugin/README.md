# Obsidian plugins

- `canvas-zoom-sensitivity`: Adjusts Canvas wheel and trackpad zoom sensitivity.
- `copy-path`: Copies the absolute path of the active Canvas for VS Code Clipper.

Each directory is an independent Obsidian plugin project. Build and install them separately.

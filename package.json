{
  "name": "vscode-clipper",
  "displayName": "vscode-clipper",
  "description": "vscode clip to draw.io",
  "version": "0.4.8",
  "repository": {
    "type": "git",
    "url": "https://github.com/sunb256/vscode-clipper.git"
  },
  "engines": {
    "vscode": "^1.125.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:vscode-clipper.stackSelection",
    "onCommand:vscode-clipper.stackPreviousGroup",
    "onCommand:vscode-clipper.clipAndPaste",
    "onCommand:vscode-clipper.pasteAll",
    "onCommand:vscode-clipper.clearStack",
    "onCommand:vscode-clipper.exportObsidianCanvas"
  ],
  "extensionKind": [
    "ui"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "vscode-clipper.stackSelection",
        "title": "Clipper: Stack Selection"
      },
      {
        "command": "vscode-clipper.stackPreviousGroup",
        "title": "Clipper: Stack Selection in Previous Group"
      },
      {
        "command": "vscode-clipper.clipAndPaste",
        "title": "Clipper: Paste Selection to draw.io"
      },
      {
        "command": "vscode-clipper.pasteAll",
        "title": "Clipper: Paste All to draw.io"
      },
      {
        "command": "vscode-clipper.clearStack",
        "title": "Clipper: Clear Stack"
      },
      {
        "command": "vscode-clipper.exportObsidianCanvas",
        "title": "Clipper: Export to Obsidian Canvas"
      }
    ],
    "keybindings": [
      {
        "command": "vscode-clipper.stackSelection",
        "key": "ctrl+shift+d",
        "when": "editorTextFocus && editorHasSelection"
      },
      {
        "command": "vscode-clipper.stackPreviousGroup",
        "key": "ctrl+shift+alt+d",
        "when": "editorTextFocus && editorHasSelection"
      }
    ],
    "configuration": {
      "title": "VS Code Clipper",
      "properties": {
        "vscode-clipper.autoHotkeyPath": {
          "type": "string",
          "default": "",
          "description": "Path to the AutoHotkey v2 executable. Leave empty to use AutoHotkey64.exe from PATH."
        },
        "vscode-clipper.drawioExecutable": {
          "type": "string",
          "default": "",
          "description": "Path to draw.io.exe. Used to start draw.io when it is not running."
        },
        "vscode-clipper.includeLineNumbers": {
          "type": "boolean",
          "default": true,
          "description": "Include selected line numbers in the pasted path label."
        },
        "vscode-clipper.includeWorkspaceFolder": {
          "type": "boolean",
          "default": true,
          "description": "Prefix labels with the workspace folder name."
        },
        "vscode-clipper.obsidian.vaultPath": {
          "type": "string",
          "default": "",
          "description": "Absolute path to the Obsidian vault used for Canvas exports."
		}
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.125.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "24.x",
    "typescript-eslint": "^8.61.1",
    "eslint": "^10.5.0",
    "typescript": "^6.0.3",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.0.0"
  },
  "overrides": {
    "diff": "^8.0.4",
    "serialize-javascript": "^7.0.6"
  }
}
